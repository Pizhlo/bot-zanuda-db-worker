
log_level: "debug"
instance_id: 1

storage:
  buffer_size: 100

  postgres:
    host: "localhost"
    port: 1111
    user: "user"
    password: "password"
    db_name: "test"    
    insert_timeout: 5000000
    read_timeout: 5000000

  redis:
    type: "single"
    host: "localhost"
    port: 6379
    insert_timeout: 50
    read_timeout: 50