
log_level: "debug"
instance_id: 1

storage:
  buffer_size: 100

# для внутреннего использования
  postgres:
    host: "localhost"
    port: 5432
    user: "user"
    password: "password"
    db_name: "test"    
    insert_timeout: 50
    read_timeout: 50